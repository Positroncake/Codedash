@page "/Codedash"
@using codedash.Client.Data
@using System.Text.Json


@code {
    private static string tempSequence = "print(\"Hi\")\u000aprint(\u001f0\u001f-1\u001e\"Hello, World!\"\u001f1\u001f13\u001e)\u001f0\u001f-1";

    protected override void OnInitialized()
    {
        base.OnInitialized();

        var pbs = ProblemBlock.ParseProblemString(tempSequence);
        Console.WriteLine(JsonSerializer.Serialize(pbs));
    }
}